# Financial Tracker Backend Configuration

# Path to the SQLite database file.
# Relative paths are resolved from the backend/ directory.
# Absolute paths are used as-is.
# Defaults to data/app.db if not set.
DB_PATH=data/app.db
