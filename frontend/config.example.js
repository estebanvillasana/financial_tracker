export const appConfig = {
    // URL of the Backend, provided by FAST API.
    apiBaseUrl: "http://127.0.0.1:8000",

    // API key for multi-user mode.
    // Must match a key in backend/users.json.
    apiKey: '',

    // Optional: override the display name shown in the sidebar.
    // If left empty, the name from users.json is fetched automatically.
    displayName: '',
  }
